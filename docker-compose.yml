version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    restart: always
    environment:
      - PORT=8000
    volumes:
      - .:/app
    # Add any other environment variables here
