from app.main import app

# This file is an entry point wrapper pointing to the newly refactored app directory.
# You can run via: uvicorn main:app --reload
# Or preferrably: uvicorn app.main:app --reload
