ctor('.item-qty').value)||0;const rate=parseFloat(row.querySelector('.item-gst').value)||0;const base=price*qty;const gst=base*(rate/100);const net=base+gst;row.querySelector('.item-total').innerText=formatCurrency(net);return{base,gst,net};};const calculateInvoiceTotal=()=>{let tBase=0,tGst=0,tNet=0;const rows=invoiceBody.querySelectorAll('.item-row');rows.forEach(row=>{const vals=calculateInvoiceRow(row);tBase+=vals.base;tGst+=vals.gst;tNet+=vals.net;});invBaseTotal.innerText="₹"+formatCurrency(tBase);invGstTotal.innerText="+ ₹"+formatCurrency(tGst);invGrandTotal.innerText="₹"+formatCurrency(tNet);};const bindRowEvents=(row)=>{const inputs=row.querySelectorAll('input, select');inputs.forEach(inp=>inp.addEventListener('input',calculateInvoiceTotal));row.querySelector('.delete-row').addEventListener('click',()=>{if(invoiceBody.querySelectorAll('.item-row').length>1){row.remove();calculateInvoiceTotal();}});};bindRowEvents(invoiceBody.querySelector('.it... (line truncated to 2000 chars)