# ─── Core (always install) ──────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
gunicorn==26.0.0
jinja2==3.1.6
pydantic==2.13.4
pydantic-settings==2.14.1
redis==5.2.1
python-multipart==0.0.32

# ─── Utilities ─────────────────────────────────────────────
qrcode==8.2
pillow==12.3.0
numpy==2.4.6
pandas==2.2.3
pyyaml==6.0.3
nh3==0.3.7
requests==2.34.2
fpdf2==2.8.7
email-validator==2.2.0
yfinance==0.2.54
prometheus_client==0.20.0

# ─── Optional heavy (pinned — installed by deploy.sh, NOT here to keep base slim) ──
# Keep versions in sync with deploy.sh HEAVY_PINS below:
# rembg[cpu]==2.2.1
# opencv-python-headless==4.10.0.84
# prophet==1.1.7
