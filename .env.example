# StoryBrain AI Configuration
# Copy to .env and fill in values

# Application
PROJECT_NAME=Multi-Tool Website

# Redis (optional — falls back to in-memory cache if unavailable)
REDIS_URL=redis://localhost:6379/0

# Gunicorn
LOG_LEVEL=info
TIMEOUT=120
KEEP_ALIVE=5
