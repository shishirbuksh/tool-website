[package]
name = "rust_predictor"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_predictor"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
rand = "0.8.5"

[profile.release]
opt-level = 3
lto = true
